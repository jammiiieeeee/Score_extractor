[pytest]
testpaths = tests
timeout = 120
addopts = -v --tb=short
markers =
    slow: tests requiring real video files or PaddleOCR (deselect with '-m "not slow"')
    integration: tests needing real filesystem I/O
    unit: pure logic tests with no I/O
