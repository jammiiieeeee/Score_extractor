---
target: app_gui.py
total_score: 32
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 3
timestamp: 2026-07-30T03-31-54Z
slug: app-gui-py
---
Method: dual-agent (A: ses_04eed6412ffet7zrygPsckbPjw · B: detect.mjs CLI scan on app_gui.py — clean, 0 findings)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Comprehensive — progress bar, status, log, watchdog. No gaps |
| 2 | Match System / Real World | 4 | "Piano Score Video to PDF" title, plain-language labels, safe-range tooltips |
| 3 | User Control and Freedom | 3 | Cancel and re-extract work. BUT: no "Reset to defaults" after Set as Default removal |
| 4 | Consistency and Standards | 3 | Mostly consistent. BUT: "⚙ Config" button label doesn't match "Settings" tab name |
| 5 | Error Prevention | 4 | Guards everywhere — disabled Extract until valid, playlist detection, overwrite confirmation |
| 6 | Recognition Rather Than Recall | 3 | Tooltips on every param, QGroupBox grouping, QCompleter — but 16 params is heavy recall |
| 7 | Flexibility and Efficiency | 2 | No keyboard shortcuts, no preset save/load, no collapsible groups |
| 8 | Aesthetic and Minimalist Design | 3 | Restructured groups help. BUT Advanced uses QGridLayout while others use QFormLayout |
| 9 | Error Recovery | 4 | try/except on all handlers, timeout watchdog, clear error messages |
| 10 | Help and Documentation | 2 | Great tooltips — but no Help menu, no inline docs, no "Learn more" link |
| **Total** | | **32/40** | **Good** |

## What Improved (Δ +18 pts from 14/40)

- **ConfigTab restructure** (H6: 0→3, H8: 1→3): 5 categorized QGroupBoxes replaced the flat 17-spinbox wall. Each section (Detection, Capture Timing, Deduplication, OCR, Advanced) has clear label + QFormLayout + tooltips on every parameter. This was the single highest-impact change.
- **Window title** (H2: 1→4): No more blank title bar. "Score Extractor — Piano Score Video to PDF" communicates brand + purpose.
- **Crop ratio deduplication** (H4: 1→3): Removed from ConfigTab; lives only in ExtractTab alongside the crop preview.
- **"Set as Default" removal** (H3: 2→3): Removed redundant button that suggested auto-save was not working.
- **Tab label** (H4): "Config" → "Settings" resolves verb/noun inconsistency.
- **Design-system drift resolved** (detector): 15 → 0 findings. Added missing colors, radii, font sizes to DESIGN.md.

## What's Still Working (inherited strengths)

1. **Crop preview drag interaction** — brass grip ridges, transparent overlay, live percentage, cursor change. Best UX in the app.
2. **Tooltip quality** — every parameter has purpose + safe range guidance. Shows real care.
3. **Progress feedback** — progress bar + status label + log + phase + status bar = excellent multi-channel system status.

## Remaining Priority Issues

### P0 — "⚙ Config" button label doesn't match "Settings" tab
Line 1327: `QPushButton("⚙ Config")` points to tab named "Settings" (line 2040). Must match: `"⚙ Settings"`.
**Suggested command**: `/impeccable clarify`

### P0 — No "Reset to defaults" after removing "Set as Default"
Auto-save is correct, but removing the only recovery path means user has no way back from a bad tweak. Add a Reset button.
**Suggested command**: `/impeccable clarify`

### P1 — Advanced group uses QGridLayout while all others use QFormLayout
Line 915-937: Advanced breaks the consistent QFormLayout pattern used by all other groups. Convert for visual consistency.
**Suggested command**: `/impeccable layout`

### P1 — Init defaults vs refresh fallback defaults disagree
`frame_check` init=0.2 (line 786), refresh fallback=0.8 (line 1023). `overlay_width` init=0.5 (line 831), fallback=0.2 (line 1028). These drift silently.
**Suggested command**: `/impeccable harden`

### P1 — OCR checkbox state not persisted
`apply_to_api` writes `ocr_conf=0` when disabled but `refresh_from_api` restores from `max(40, value)` with no checkbox state. Save as a separate key.
**Suggested command**: `/impeccable harden`

## Minor Observations

- The ConfigTab is now 16 params + 2 checkboxes across 5 sections — improved but still dense. Collapsible sections or a "Show advanced" toggle would be the next step.
- 4 dedup thresholds have overlapping names and 0-1 ranges — tooltips help but they blur without visual cues.
- ⚙ icon is a Unicode character — may not render on all systems. Consider QIcon.

## Questions to Consider

1. **"⚙ Config" vs "Settings" tab** — two-word fix. Want it?
2. **Reset to defaults** — should this reset the current tab only or the entire app config?
